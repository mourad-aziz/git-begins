Cloner un repo
git clone https://github.com/mourad-aziz/git-begins.git
 
Les 3 commandes de push (dans l’odre)
    git init
    git add 
    git commit -m "Add existing project files to Git"
    git push -u -f origin master.
